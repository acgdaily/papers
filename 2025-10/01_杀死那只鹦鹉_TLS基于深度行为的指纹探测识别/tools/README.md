# 工具包

- `client_behavior_detector`

用于生成各种畸形包以测试客户端行为

- `replay_behavior_detector`

用于重放数据包并配合 `ChangeCiperSpec` 进行行为对比测试 (`Warning Alert` 测试方法同样, 以此类推)

- `server_behavior_detector`

用于生成各种畸形包以测试服务器行为
